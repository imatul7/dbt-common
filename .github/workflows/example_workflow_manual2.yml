name: Manual Workflow on repo Changes

on:
  workflow_dispatch:
  
jobs:
  check_changes:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v3
      - name: Check for changes in dbt_common
        id: check_dbt_common
        run: |
          if git diff --name-only HEAD^ HEAD | grep -q 'Test_folder/**'; then
            echo "dbt_common_changed=true" >> $GITHUB_ENV
          else
            echo "dbt_common_changed=false" >> $GITHUB_ENV
          fi

  your_job:
    needs: check_changes
    runs-on: ubuntu-latest
    if: env.dbt_common_changed == 'true'
    steps:
      - uses: actions/checkout@v3
      - name: Run a script or command
        run: echo "Changes detected in repo dbt_common, proceeding with job..."
      # Add your subsequent steps here. They will only run if changes were detected in dbt_common.
